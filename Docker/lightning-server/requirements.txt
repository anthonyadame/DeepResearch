# ========== Core Agent Lightning Installation ==========
agentlightning>=0.2.0

# APO (Automatic Prompt Optimization) support
agentlightning[apo]

# VERL (Volcano Engine Reinforcement Learning) support - Manual installation recommended
agentlightning[verl]

# MongoDB support for persistent storage
agentlightning[mongo]

# ========== PyTorch Dependencies ==========
# For CPU-only, use: --index-url https://download.pytorch.org/whl/cpu
# For CUDA 12.4, use: --index-url https://download.pytorch.org/whl/cu124
torch==2.4.0
torchvision==0.19.0

# ========== VERL-Specific Dependencies ==========
# flash-attention (required for VERL, CUDA only)
flash-attn>=2.5.0
# vLLM for LLM serving (primary serving engine)
vllm==0.6.3.post1
# VERL framework
verl==0.5.0

# ========== FastAPI and Server Dependencies ==========
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# ========== HTTP Client ==========
httpx==0.26.0

# ========== LLM Client SDKs ==========
# OpenAI SDK (required for APO, version 2.0+)
openai>=2.0.0
# Anthropic SDK (optional, for Claude models)
anthropic>=0.18.0

# ========== LLM Proxy and Serving ==========
# LiteLLM for unified LLM proxy
litellm>=1.40.0
# Additional LiteLLM dependencies
aiohttp>=3.9.0

# ========== MongoDB Dependencies ==========
# Motor - async MongoDB driver
motor>=3.3.0

# ========== OpenTelemetry Dependencies ==========
# Core OpenTelemetry packages
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
# OTLP Exporter (for sending traces to collector)
opentelemetry-exporter-otlp>=1.20.0
# Instrumentation libraries
opentelemetry-instrumentation-fastapi>=0.41b0
opentelemetry-instrumentation-httpx>=0.41b0
# Resource detection
opentelemetry-resourcedetector>=0.1.0
# PyMongo - sync MongoDB driver (used by motor)
pymongo>=4.6.0

# ========== OpenTelemetry Dependencies ==========
# Core OpenTelemetry SDK
opentelemetry-api>=1.21.0
opentelemetry-sdk>=1.21.0
# OTLP exporters for traces
opentelemetry-exporter-otlp>=1.21.0
opentelemetry-exporter-otlp-proto-http>=1.21.0
# Instrumentation for automatic tracing
opentelemetry-instrumentation>=0.42b0
opentelemetry-instrumentation-fastapi>=0.42b0
opentelemetry-instrumentation-httpx>=0.42b0

# ========== Monitoring and Logging ==========
python-json-logger==2.0.7
prometheus-client==0.19.0
psutil==5.9.8

# ========== NLP Libraries ==========
numpy==1.26.3
nltk==3.8.1
textblob==0.17.1

# ========== Development and Testing ==========
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-mongodb>=3.0.0

# ========== Optional: Ollama Support ==========
# Uncomment if using Ollama as alternative LLM serving backend
# ollama>=0.1.0